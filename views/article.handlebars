{{#if article}}
  <article>
    <h1>{{article.title}}</h1>
    <p>{{article.author}} - {{formatDate article.date}}</p>
    <p>{{article.content}}</p>
  </article>
{{else}}
  <h1>Article not found</h1>
{{/if}}

